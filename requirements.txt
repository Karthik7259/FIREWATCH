# Flask web framework
Flask==3.0.0

# MongoDB driver for Python
pymongo==4.6.1

# HTTP library for making requests
requests==2.31.0

# Flask CORS extension for handling Cross-Origin Resource Sharing
Flask-Cors==4.0.0

# BSON library (usually comes with pymongo, but listing for clarity)
# bson is included with pymongo

# Additional common dependencies for Flask applications
Werkzeug==3.0.1
Jinja2==3.1.2
MarkupSafe==2.1.3
itsdangerous==2.1.2
click==8.1.7

# For production deployment (optional)
gunicorn==21.2.0

# For development and testing (optional)
python-dotenv==1.0.0
